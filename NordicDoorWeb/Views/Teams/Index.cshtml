@model IEnumerable<NordicDoorWeb.Models.Teams>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.team)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.team)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.teams_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.teams_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.teams_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
