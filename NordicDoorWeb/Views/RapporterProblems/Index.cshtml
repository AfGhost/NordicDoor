@model IEnumerable<NordicDoorWeb.Models.RapporterProblem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ansatt_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.problem_tittel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.problem_tekst)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ansatt_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.problem_tittel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.problem_tekst)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.rproblem_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.rproblem_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.rproblem_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
